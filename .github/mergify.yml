# More information on Mergify is available at:
# https://docs.mergify.io/
pull_request_rules:
  - name: automatic merge for Dependabot pull requests
    conditions:
      - author~=^dependabot(|-preview)\[bot\]$
      - check-success=buildDeploy
    actions:
      merge:
        method: merge
